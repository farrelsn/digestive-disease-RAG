# Secrets — never commit these. The Groq key lives in the Space's own secrets UI.
.env
groq

# Built artefacts. data/chroma_db/ is rebuilt by the Dockerfile from data/chunks.jsonl,
# and health_rag_data/ is the notebook's download cache.
data/chroma_db/
health_rag_data/

__pycache__/
*.pyc
.venv/
